**【PDF2Tree リリース前チェックリスト】**

*   [ ] **ビルド構成の確認**: IDEのプロジェクトマネージャで、ビルド構成が `Release` になっているか確認する。（`Debug` のまま配布すると、ファイルサイズが肥大化し、実行速度も低下する）
*   [ ] **バージョン情報の更新**: プロジェクトオプションの「バージョン情報」が最新の数値になっているか確認する。
*   [ ] **不要な uses の排除**: `System.Skia` や `Vcl.Skia` など、開発中にテストで追加した不要なユニットが残っていないか確認する。（ユーザー環境でのDLL欠落エラーを防ぐため）
*   [ ] **デバッグコードの無効化**: `{$IFDEF DEBUG}` で囲まれていない、一時的な `ShowMessage` やローカルパス（`C:\test.txt` 等）への保存処理が残っていないか、ソース全体を検索して確認する。
*   [ ] **ドキュメントの更新**: `ReadMe.txt`（同梱用）、`README.md`（GitHub用）、`Delphi.md` の更新履歴や説明文が最新の状態になっているか確認する。
*   [ ] **配布用ZIPの構成確認**: 作成したZIPファイルの中に、以下の3点**のみ**が含まれているか確認する。（不要な `.dcu` やテスト用PDFが混入していないか）
    *   `PDF2Tree.exe` (Release版)
    *   `pdfium.dll`
    *   `ReadMe.txt`
